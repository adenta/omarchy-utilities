#!/bin/bash
# Source: https://github.com/adenta/omarchy-utilities — read AGENTS.md; update XPS and Grace.
set -euo pipefail

[[ ${1:-} == "catppuccin-latte" ]] || exit 0
[[ $(cat "$HOME/.local/state/omarchy/current/theme.name" 2>/dev/null) == "catppuccin-latte" ]] || exit 0

color_fade="$HOME/.local/state/omarchy/current/theme/backgrounds/1-color-fade.png"
[[ ! -e $color_fade ]] || rm -f -- "$color_fade"
